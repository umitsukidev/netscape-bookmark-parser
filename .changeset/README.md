# Changesets

Add a changeset for each user-facing change with `pnpm changeset`. The release
workflow opens a version pull request on `main`, and the release workflow
publishes versions merged from that pull request.
